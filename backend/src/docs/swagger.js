const swaggerJsdoc = require('swagger-jsdoc');

const options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Crypto Portfolio Tracker API',
      version: '1.0.0',
      description: 'REST API for managing crypto portfolios, assets, auth and analytics.',
    },
    servers: [{ url: '/api/v1' }],
    components: {
      securitySchemes: {
        bearerAuth: { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
      },
    },
    security: [{ bearerAuth: [] }],
  },
  apis: ['./src/docs/paths/*.yaml'],
};

module.exports = swaggerJsdoc(options);
